apiVersion: v1
kind: Service
metadata:
  name: custom-nginx
  labels:
    app: custom-nginx
spec:
  type: NodePort
  selector:
    app: custom-nginx
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080  # Port fixe pour faciliter l'accès
